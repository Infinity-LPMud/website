---
import { SEO } from "astro-seo";
import "../../styles/global.css";
import ArchiveBreadcrumb from "../components/ArchiveBreadcrumb.astro";
import SEOHead from "../components/SEOHead.astro";
interface Props {
  breadCrumbs: { href: string; label: string }[];
  title: string;
}
const { breadCrumbs, title } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <SEOHead title={title} />
  </head>
  <body class="archive">
    {
      breadCrumbs && (
        <ArchiveBreadcrumb breadCrumbs={breadCrumbs} title={title} />
      )
    }

    <main class="min-h-screen">
      <div class="pt-10 text-sm text-black max-w-800 mx-auto">
        <slot />
      </div>
    </main>
    <!-- <script
      is:inline
      src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/1.8.0/flowbite.min.js"
    ></script> -->
  </body>
</html>
